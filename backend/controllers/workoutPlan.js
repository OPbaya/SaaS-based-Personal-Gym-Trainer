export const generateWorkoutPlanHTML = (contentFromBackend) => {
    // This function takes the complete HTML document string from your backend
    // and returns it directly. This is the correct approach because your
    // data is already in the final HTML format.

    return contentFromBackend;
};